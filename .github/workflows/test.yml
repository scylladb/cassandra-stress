name: "Test Cassandra-Stress"

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    uses: "./.github/workflows/build.yml"
    with:
      version: '1.0.0'
  test-rpm:
    needs: build
    strategy:
      matrix:
        os: [ 'fedora:41', 'fedora:42', 'rockylinux/rockylinux:9-ubi-micro' ]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: cassandra-stress
      - run: |
          docker run --rm -v ${{ github.workspace }}/build:/mnt -w /mnt ${{ matrix.os }} bash -c "yum install -y ./cassandra-stress-dev-1.noarch.rpm && cassandra-stress --version"

  test-deb:
    needs: build
    strategy:
      matrix:
        os: [ 'ubuntu:22.04', 'ubuntu:24.04', 'debian:12-bookworm-slim' ]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: cassandra-stress
      - run: |
          docker run --rm -v ${{ github.workspace }}/build:/mnt -w /mnt ${{ matrix.os }} bash -c "apt-get install -y ./cassandra-stress-dev_1_all.deb && cassandra-stress --version"

  build-test:
    strategy:
      matrix:
        java: [ '21', '23' ]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java }}
      - name: Setup Ant
        uses: cedx/setup-ant@v4
        with:
          optional-tasks: true
          version: latest
      - name: Setup
        run: ant init
      - name: Test tests
        run: ant build-test
